/**
 * Photo Reminder App
 * https://github.com/facebook/react-native
 * @flow
 */

import React, { Component } from 'react';
import { AppRegistry } from 'react-native';
import PhotoReminder from './app/App'

AppRegistry.registerComponent('PhotoReminder', () => PhotoReminder);
